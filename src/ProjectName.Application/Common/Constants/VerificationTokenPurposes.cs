namespace ProjectName.Application.Common.Constants;

public static class VerificationTokenPurposes
{
    public const string VerifyOwner = nameof(VerifyOwner);
    public const string PasswordRecovery = nameof(PasswordRecovery);
}